import { useState } from 'react';
import PageHeader from '../components/PageHeader';
export default function MediaLibraryPage() { const [files, setFiles] = useState([]); return <div className="page"><PageHeader title="Media Library" subtitle="Upload images and videos used across the public site" /><section className="panel"><label className="upload-zone">Drag files here or choose from your computer<input type="file" accept="image/*,video/*" multiple onChange={(event) => setFiles(Array.from(event.target.files))} /></label><div className="media-preview-grid">{files.map((file) => <article className="media-preview" key={`${file.name}-${file.lastModified}`}><strong>{file.type.startsWith('video/') ? 'Video' : 'Image'}</strong><span>{file.name}</span><small>{Math.round(file.size / 1024)} KB</small></article>)}</div></section></div>; }
