import PageHeader from '../components/PageHeader';
export default function FacilitiesPage() { return <div className="page"><PageHeader title="Facilities" subtitle="Manage sports venues, capacity, and images" action={() => window.alert('Create facility form can be connected to Supabase here.')} actionLabel="New Facility" /><div className="facility-admin-grid">{['Kepler Arena', 'Olympic Pool', 'North Training Field'].map((name) => <article className="facility-admin-card" key={name}><div className="facility-admin-card__preview">🏟️</div><div className="facility-admin-card__body"><h3>{name}</h3><p className="muted">Court · Capacity —</p><button className="btn btn--small">Edit facility</button></div></article>)}</div></div>; }
